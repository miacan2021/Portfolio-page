import { Nav } from "./Nav.styled"

export const NavBar = () => {
    return(
            <Nav>
                <div>Logo</div>
                <div>Menu</div>
                <div>Nav list</div>
            </Nav>

    )
}